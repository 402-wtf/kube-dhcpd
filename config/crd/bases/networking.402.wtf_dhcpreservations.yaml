---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: dhcpreservations.networking.402.wtf
spec:
  group: networking.402.wtf
  names:
    kind: DHCPReservation
    listKind: DHCPReservationList
    plural: dhcpreservations
    singular: dhcpreservation
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DHCPReservation is the Schema for the dhcpreservations API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DHCPReservationSpec defines the desired state of DHCPReservation
            properties:
              ipAddress:
                description: IPAddress is the reserved IP address
                type: string
            required:
            - ipAddress
            type: object
          status:
            description: DHCPReservationStatus defines the observed state of DHCPReservation
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
